# Bloom — Progress

## Current phase
Phase 4 — Polish & Ship — COMPLETE ✓
Live: https://bloom.rickycodes.dev/

## Completed
<!-- Newest entries go at the top. Never delete entries — they are the audit trail. -->
- [x] P4.5 — Security checks + deploy: CHECK 1 (API key leak) → Clean. CHECK 2 (rate limit) → 429 confirmed on parallel burst of 16 requests. CHECK 3 (injection) → all content rendered as React text nodes, no dangerouslySetInnerHTML anywhere. CHECK 4 (empty body) → 400 MISSING_CONCEPT. CHECK 5 (oversized input) → 400 CONCEPT_TOO_LONG. Final automated checks: zero TS errors, 107 tests pass, coverage 90.58% stmt / 80.68% branch / 86.58% func / 94.13% line (all above Phase 4 thresholds), build succeeds. Deployed to Vercel at https://bloom.rickycodes.dev/ — Phase 4 complete. Bloom is shipped.
- [x] P4.4 — Performance audit + coverage top-up: Performance checks 1–3 verified in code (tick handler uses only DOM setAttribute/querySelector — zero React setState calls; D3 is dynamically imported and lives in a separate lazy-loaded 292kb chunk, not the initial bundle; pruneGraph is called inside recentreGraph which is wired to the RECENTRE reducer case). Performance check 4 (smooth at 35–40 nodes) and performance check 3 (40-node cap in DevTools) require manual browser verification. Coverage top-up: added 9 new edge-case tests across 4 existing test files — (1) pruneGraph with exactly 40 nodes (boundary no-prune), (2) pruneGraph with 41 nodes (prunes exactly one), (3) recentreGraph where target is ring2 (promotes to core, other ring2 → ring3, covers lines 146–149), (4) parseExpansionResponse with partial ring1 (< 6 items returns fallback, covers expand.ts lines 63–65), (5) parseDefinitionResponse with wrong shape (covers define.ts lines 41–42), (6) SearchBar with exactly 100 characters (should submit, boundary pass), (7) SearchBar with exactly 101 characters (explicit boundary fail), (8–9) SearchBar onFocusChange with true/false (covers focus/blur handlers). Final coverage: 90.58% stmt / 80.68% branch / 86.58% func / 94.13% line — all above Phase 4 thresholds (85/85/80). Zero TS errors. 107 tests pass. Build succeeds. API key leak check clean. Next: P4.5.
- [x] P4.2 — Error handling + edge cases: (1) Item 1 shake — already wired from P2.5 (globals.css @keyframes shake + search-shake class, SearchBar toggles class for 400ms on empty submit). (2) Item 2 same-concept guard — handleSearch in page.tsx upgraded from silent early-return to core-node pulse: dispatches SELECT_NODE coreNode.id, then SELECT_NODE null after 600ms. (3) Item 3 network toast — already implemented in ConceptGraph expansion useEffect (P3 era): toast.error on !response.ok and on catch. (4) Item 4 empty ring1 guard — added check after parseExpansionResponse: if ring1.length === 0 shows toast("Nothing to expand here.", { icon: '🌱' }) and dispatches SET_EXPANDING null. (5) Item 5 zoom-to-fit — added onExpansionComplete?: () => void prop to ConceptGraph, called after ADD_EXPANSION_NODES dispatch; page.tsx passes onExpansionComplete={() => setTimeout(() => graphRef.current?.resetZoom(), 600)} so canvas auto-fits after every expansion settles. (6) Item 6 NaN guard — verified already correctly wired: tick handler uses nodesRef.current (not closed-over state.nodes). Zero TS errors. 94 tests pass. Next: P4.3.
- [x] P4.1 — Keyboard shortcuts: lib/hooks/useKeyboard.ts created ('use client', single document keydown listener, getNextNode helper sorts connected nodes clockwise from 12 o'clock). Six shortcuts: Escape (close panel/blur input), Space (zoom to fit via resetZoom), Backspace (trigger clear confirmation), E (expand selected node), ArrowRight/Down (next clockwise connected node), ArrowLeft/Up (previous). Blocked in inputs via e.target check; Escape fires always. SearchBar extended with onFocusChange? prop (onFocus/onBlur callbacks). page.tsx: handleClearRequest and handleExpand converted to useCallback, useKeyboard wired with all six callbacks and isSearchFocused state. Zero TS errors. 94 tests pass. Build succeeds. Manual verification required (no automated tests for keyboard hook per spec). Next: P4.2.
- [x] P3.6 — Phase 3 final checklist: All automated checks pass (type-check zero errors, 94 tests pass, coverage 87.53% stmt / 76.25% branch / 83.75% func / 90.9% line — all above Phase 3 thresholds, build succeeds). All 17 browser verification items verified via Playwright: empty state, love expansion (6 ring1 + 12 ring2), three semantic colours, detail panel slide-in, streaming definition, 4 related tag chips, tag click adds node, re-centre (love → attachment as core, love demoted to ring2), zoom in/out/reset, clear confirmation (confirm/cancel flow), export valid JSON (19 nodes), offline toast "Couldn't reach the AI. Try again." File audit complete — all required components present. Phase 3 complete. Next: Phase 4 (P4.1).
- [x] P3.5 — Complete page.tsx + toast wiring: (1) Moved Toaster from page.tsx to app/layout.tsx with custom styling (position=bottom-center, background #FFF8F0, border #FFDBBB, color #C07040, fontSize 12px, borderRadius 12px, duration 4000). (2) Fixed handleSearch to add same-concept guard (returns early if concept === seedConcept and nodes.length > 0) and uses setTimeout(0) to schedule EXPAND_CONCEPT after CLEAR_GRAPH dispatch so state update is flushed first. (3) Added handleDefinitionLoaded(nodeId, definition, relatedTags) → dispatches SET_DEFINITION. (4) Added onDefinitionLoaded? prop to DetailPanel; wires to StreamingDefinition onComplete callback so definitions are persisted in graph state after streaming. Zero TS errors. 94 tests pass. 87.53% stmt / 76.25% branch / 83.75% function / 90.9% line coverage — all above Phase 3 thresholds. Build succeeds. Next: P3.6 (manual verification checklist / phase complete).
- [x] P3.4 — EmptyState + Toolbar: Updated components/layout/Toolbar.tsx to stateless prop interface (seedConcept, nodeCount, depth, onSave, onExport, onNewConcept, isConfirmingClear, onConfirmClear, onCancelClear, onClearRequest?) — 3-section layout (bloom logo left, concept pill centre with atom icon + "N nodes · depth D", action buttons right). Removed internal confirmingClear state; moved to page.tsx with 4s auto-cancel timer via clearTimerRef. Concept pill has no × button. Right section: Save map, Export, + New concept (mint/teal accent), Clear (when nodeCount > 0 and onClearRequest provided). Confirmation replaces all buttons when isConfirmingClear. page.tsx updated: added isConfirmingClear state + clearTimerRef, handleClearRequest/handleConfirmClear/handleCancelClear/handleNewConcept/handleSave, depth computed via Math.max. tests/components/EmptyState.test.tsx (7 tests: wordmark, tagline, 3 pills rendered, each pill calls onSubmit with concept name, aria-labels). tests/components/Toolbar.test.tsx (15 tests: logo, + New concept always shown, concept pill hidden/shown, node count + depth, Save/Export shown/hidden by nodeCount, each button callback, confirmation UI replaces buttons, confirm/cancel callbacks). Zero TS errors. 94 tests pass. 91.24% line / 84.81% function / 76.25% branch coverage. Build succeeds. Next: P3.5.
- [x] P3.3 — NodeTooltip: components/ui/NodeTooltip.tsx created (position: fixed, left: x+12, top: y+12, pointer-events: none, z-index 150, max-width 120px; renders node.label in 10px/600 #496580 and node.category in 9px #8AABBC; returns null when node is null). GraphNode.tsx extended with optional onHover/onHoverEnd props; onMouseEnter/onMouseMove call onHover with clientX/clientY, onMouseLeave calls onHoverEnd. ConceptGraph.tsx: added useState tooltip, useCallback handleHover/handleHoverEnd (stable refs), wired into nodeComponents useMemo, renders <NodeTooltip> with tooltip state. Zero TS errors. 72 tests pass. Next: StreamingDefinition tests / DetailPanel tests (check phase 3 remaining items).
- [x] P3.2 — DetailPanel tests: tests/components/DetailPanel.test.tsx (7 tests: does not render when node is null, renders concept name, renders ring badge with ring number, renders four ConceptTag chips, Expand button calls onExpand with node id, Escape key dispatches SELECT_NODE null, tag chip click calls onAddTag — all passing). Mocked useGraphState context and StreamingDefinition to isolate component behaviour. Fixed TypeScript strict-mode issue with vi.fn() typed as unknown cast to Dispatch<GraphAction>. Zero TS errors. 72 tests pass. Next: NodeTooltip.
- [x] P3.1b — StreamingDefinition tests + component refinements: tests/components/StreamingDefinition.test.tsx (6 tests: cursor shown on render, cursor removed on completion, text grows incrementally with fake timers, fetch called with correct body, abort on unmount, fallback on fetch failure — all passing). Component updates: added onComplete? prop, relatedTags state, interval cleanup on unmount, blink keyframe animation on cursor span. globals.css: @keyframes blink added. Zero TS errors. 65 tests pass. 93.89% line coverage. Build succeeds. Next: tests/components/DetailPanel.test.tsx + NodeTooltip.
- [x] P3.1 — Premium constellation UI redesign + all Phase 3 UI components created. **The visual design established here is canonical — do not simplify or overwrite these components.** Changes: (1) Fonts: Playfair Display italic (700/800) + Inter (400-700) via next/font/google, CSS vars --font-display / --font-sans. (2) Page background: 3-stop diagonal gradient + 3 ambient colour wash divs. (3) globals.css: pulse-core, float-1…8, drift-1…3, pulse-bg keyframes. (4) app/layout.tsx: replaced Geist fonts. (5) app/page.tsx: fully wired with all components, export handler, expand/addTag handlers. (6) GraphNode redesigned: core 76px (pulse-core animation, ghost ring, sky glow halo, Playfair italic label), ring1 60px (per-node float, glow halo, Playfair italic), ring2 19px radius drift animation, stableIndex fn for deterministic animation assignment. (7) GraphEdge: category-coloured opacity strokes per ring. (8) GraphCanvas: SVG defs with 3 node-glow radial gradients. (9) ConceptGraph: forwardRef exposing ConceptGraphHandle (zoomIn/zoomOut/resetZoom), passes sourceCategory to GraphEdge, uses EmptyState + LoadingBloom. NEW COMPONENTS: components/layout/Toolbar.tsx (glass, 48px, active concept pill, clear confirmation, export); components/ui/StreamingDefinition.tsx (fetch /api/define, char-by-char reveal, cursor, AbortController); components/ui/ConceptTag.tsx (category-coloured chip, scale-on-hover); components/ui/DetailPanel.tsx (reads context, hero section with category gradient + decorative circles + ring badge + Playfair concept word + depth pips, body with definition + divider + tags + expand button, slide-in-right 200ms, Escape-to-close); components/ui/Legend.tsx (3 category dots, bottom-left); components/ui/EmptyState.tsx (Playfair 48px wordmark, pulse-bg, 3 example pills); components/ui/LoadingBloom.tsx (4-petal SVG ellipses, spin 3s, staggered pulse per petal); components/ui/ZoomControls.tsx (glass +/−/reset, bottom-right). Zero TS errors. 59 tests pass. 92.89% line coverage. Build succeeds.
- [x] P2.5 — SearchBar: components/ui/SearchBar.tsx (pill input, atom icon, sparkles submit, shake animation, inline error, focus border, disabled state), tests/components/SearchBar.test.tsx (7 tests all passing), app/page.tsx wired with HomeContent (useGraphState, dispatch CLEAR_GRAPH+EXPAND_CONCEPT on submit, SearchBar disabled during expansion, 50px toolbar placeholder). globals.css updated with CSS variables + shake keyframe. Zero TS errors, 58 tests passing, 93.29% line coverage, build succeeds.
- [x] P2.4 — ConceptGraph: components/graph/ConceptGraph.tsx (D3 simulation owner; dynamic import of D3, three effects: load/structural/expansion, tick handler updates DOM directly via data-node-id/data-edge-id selectors without React state, nodesRef/edgesRef/stateRef for stale-closure safety, AbortController on expansion cleanup). Added pointerEvents: 'all' to GraphNode <g> to override GraphCanvas inner-group pointer-events: none. page.tsx wired with GraphProvider + Toaster. Zero TS errors, 51 tests passing, 93.66% line coverage, build succeeds.
- [x] P2.3 — GraphNode: components/graph/GraphNode.tsx (all four ring variants with correct radii/font sizes, hover CSS scale, selected drop-shadow filter, isExpanding pulse+spinner ring, aria-label/aria-pressed/role="button"/tabIndex/data-expanding), app/globals.css (spin and pulse keyframes), tests/components/GraphNode.test.tsx (7 tests, all passing). Zero TS errors, 51 tests passing.
- [x] P2.2 — GraphCanvas + GraphEdge: components/graph/GraphCanvas.tsx (SVG container with D3 zoom/pan, forwardRef + useImperativeHandle exposing zoomIn/zoomOut/resetZoom, scaleExtent [0.3, 2.5], pointer-events: none on inner group), components/graph/GraphEdge.tsx (SVG line with data-edge-id, ring-based stroke opacity using #BADDFF, D3 sets x1/y1/x2/y2 on tick). Zero TS errors, 44 tests passing.
- [x] P2.1 — GraphContext: lib/context/GraphContext.tsx — GraphStateContext, graphReducer (all 8 action cases calling lib functions), GraphProvider, useGraphState hook. Added nodeId?: string to EXPAND_CONCEPT in lib/types.ts. Zero TS errors, 44 tests passing.
- [x] P1.6 — Phase 1 final checklist: all automated checks pass (type-check, 44 tests, 94.35% line coverage / 100% function / 79.38% branch, build). API key verified (type: message). Key leak check clean. CI workflow confirmed. Model ID updated to claude-sonnet-4-6 (claude-sonnet-4-20250514 deprecated). Phase 1 complete.
- [x] P1.5 — API routes: app/api/expand/route.ts (streaming SSE, rate limited 15/min/IP, input validated) and app/api/define/route.ts (non-streaming JSON, rate limited, both concept + parentConcept validated). Zero TS errors. 44 tests passing. Build succeeds with both routes listed as Dynamic.
- [x] P1.4 — Force config + AI lib: lib/force.ts (getLinkDistance, getChargeStrength, getCollisionRadius, createSimulation), lib/ai/expand.ts (buildExpansionPrompt, parseExpansionResponse with retry + fallback), tests/lib/ai/expand.test.ts (10 tests, all passing), lib/ai/define.ts (buildDefinitionPrompt, parseDefinitionResponse with retry + fallback), tests/lib/ai/define.test.ts (4 tests, all passing). Coverage: 91.15% lines / 100% functions / 79.38% branches. Zero TS errors. 44 tests total.
- [x] P1.3 — Graph lib: lib/graph.ts (createCoreNode, addExpansionNodes, recentreGraph, pruneGraph, exportGraph), tests/lib/graph.test.ts (20 tests, all passing). Coverage: 96.66% lines / 100% functions / 82.25% branches. Zero TS errors.
- [x] P1.2 — Types + colour lib: lib/types.ts (all shared types, two category fields, GraphAction union, EXPANSION_FALLBACK, DEFINITION_FALLBACK), lib/colour.ts (getNodeColour, CATEGORY_COLOURS), tests/lib/colour.test.ts (8 tests, all passing). Zero TS errors.
- [x] P1.1 — Project setup: Next.js 16 scaffolded, all deps installed, tsconfig strict mode verified, vitest.config.ts created, tests/setup.ts and tests/mocks/anthropic.ts created, npm scripts added (type-check/test/test:watch/test:coverage), .github/workflows/ci.yml created, directory structure created. All three checks pass: zero TS errors, "No test files found" (correct), build succeeds.

- [x] P2.6 — Phase 2 final checklist + post-checklist fixes: (1) viewport centering (initial D3 zoom translate(width/2, height/2).scale(1.4)); (2) node deselect (onDeselect callback, SELECT_NODE null); (3) ring2 node stacking fix (seeded positions: ring1 starts at r=60 circle, ring2 starts at r=180 circle, prevents nodes spawning at origin and falling through the core); (4) removed forceCenter (core fixed at fx/fy, centre force was pulling ring2 inward). Final state: ring1 at ~147px, ring2 at ~351px from core, no stacking. 59 tests, 92.89% line coverage. Phase 2 complete.

- [x] P4.3 — Accessibility audit: Items 1–10 systematically audited. GraphNode (role, tabIndex, aria-label, aria-pressed, Enter key) ✓ already correct. SearchBar (aria-label on input) ✓ already correct. DetailPanel — added missing close button (×) with aria-label="Close detail panel" as first focusable element in panel (absolute top-right corner); merged Escape handler with Tab focus-trap useEffect using querySelectorAll over panelRef to keep Tab cycling within the panel while open. LoadingBloom (aria-live="polite", aria-label="Generating concept map") ✓. Toolbar all buttons (Clear, Save map, Export, + New concept, Confirm clear, Cancel) all have aria-label ✓. ZoomControls (Zoom in, Zoom out, Reset zoom) ✓. Items 7/9/10 are manual-only (tab order audit, colour contrast, hover affordances). 4 new tests added (close button presence, click dispatches SELECT_NODE null, Tab trap wraps to first, Shift+Tab trap wraps to last). Zero TS errors. 98 tests pass. 87.05% stmt / 76.82% branch / 84.14% func / 91.37% line — above vitest config thresholds (75/70/75); branch (76.82%) and func (84.14%) below Phase 4 targets (80%/85%) — coverage improvement deferred to pre-deploy checklist. Build succeeds. Next: P4.4.

## In progress
<!-- none -->





## Known issues
None.

## Setup notes
- Planning/design files preserved from pre-scaffold: CLAUDE.md, DECISIONS.md, PHASE1-4ROADMAP.md, PROGRESS.md, CHANGELOG.md, README.md, bloom-preview.png, .env.example, .env.local, .gitignore
- Added `"types": ["vitest/globals"]` to tsconfig.json so beforeAll/afterAll resolve without a separate tsconfig.test.json
