# Anthropic API — console.anthropic.com → API Keys → Create new key
# Starts with "sk-ant-". Used only in server-side route handlers.
# If missing or invalid, /api/expand and /api/define return 401.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
